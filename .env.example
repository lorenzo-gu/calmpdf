# Google AdSense — leave both unset to disable ads entirely (dev/preview default).
# Once your AdSense account is approved and you have a publisher ID and an
# ad unit slot ID, set both vars in production (e.g. Vercel env vars).
NEXT_PUBLIC_ADSENSE_CLIENT=
NEXT_PUBLIC_ADSENSE_ARTICLE_SLOT=
