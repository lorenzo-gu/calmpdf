import type { Metadata } from "next";
import { SITE } from "@/lib/site";
import { BLOG_POSTS } from "@/content/posts";
import { ArticleShell } from "@/components/ArticleShell";

const post = BLOG_POSTS.find((p) => p.slug === "pdf-to-word-not-working")!;
export const metadata: Metadata = { title: "PDF to Word Not Working? | CalmPDF Troubleshooting", description: "Fix common PDF to Word conversion issues: scanned PDFs, blank DOCX output, formatting loss, and conversion failures.", keywords: ["pdf to word not working", "pdf to docx failed", "convert pdf to editable word document"], alternates: { canonical: `${SITE.url}/how-to/pdf-to-word-not-working` } };
export default function Page() { return <ArticleShell post={post}><h1 className="text-3xl font-semibold tracking-tight mb-4">PDF to Word Not Working? Fixes That Usually Help</h1><p className="text-sage-700 mb-6">If your <strong>PDF to Word</strong> conversion failed, produced a blank file, or broke layout, use this quick checklist before trying again.</p><p className="text-sage-700 mb-6"><a href="/pdf-to-docx" className="text-sage-900 underline underline-offset-4">Retry with CalmPDF&apos;s PDF to Word tool.</a></p><h2 className="text-2xl font-semibold mt-8 mb-3">1) Check whether your PDF is scanned</h2><p className="text-sage-700 mb-6">Try selecting text in the PDF. If selection does not work, your file is likely image-only and needs OCR. CalmPDF does not support OCR yet, so scanned PDFs are not currently convertible to editable DOCX.</p><h2 className="text-2xl font-semibold mt-8 mb-3">2) Confirm your expectations about formatting</h2><p className="text-sage-700 mb-6">PDFs are fixed-layout pages, while Word documents are built for editing and reflow. It is normal for tables, columns, headers, footers, forms, and positioned images to shift.</p><h2 className="text-2xl font-semibold mt-8 mb-3">3) Use smaller or simpler files first</h2><p className="text-sage-700 mb-6">Browser-based conversion uses local device memory. Very large PDFs or complex designs can fail on low-memory devices. Test with a short section first to verify conversion quality.</p><h2 className="text-2xl font-semibold mt-8 mb-3">4) DOCX is expected (not old .doc)</h2><p className="text-sage-700 mb-6">"Word document" usually means <strong>.docx</strong> today. CalmPDF outputs DOCX only and does not generate legacy <strong>.doc</strong> files.</p><p className="text-sage-700 mb-6">Need related tools? Try <a href="/docx-to-pdf" className="text-sage-900 underline underline-offset-4">DOCX to PDF</a>, <a href="/edit-pdf" className="text-sage-900 underline underline-offset-4">Edit PDF</a>, <a href="/compress-pdf" className="text-sage-900 underline underline-offset-4">Compress PDF</a>, or <a href="/merge-pdf" className="text-sage-900 underline underline-offset-4">Merge PDF</a>.</p></ArticleShell>; }
